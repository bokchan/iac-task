on:
  pull_request:
   types:
     - opened
     - synchronize
     - reopened

jobs:
  call-code-qa:
    uses: bokchan/iac-task/.github/workflows/qa_reusable.yml@set-up-qa-tools
    with:
      target_directory: infra
