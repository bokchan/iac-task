[project]
name = "webapp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"

[dependency-groups]
app = [
    "fastapi[standard]>=0.120.0",
]
dev = [
    "pyrefly>=0.38.2",
    "ruff>=0.14.2",
]
